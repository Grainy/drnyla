{% extends "base.twig" %}
{% block content %}
	<div class="l-page-section">
		<div class="container">
			<div class="row">
				<div class="col-md-8 col-md-offset-2 l-tertiary">
					<h1 class="b-secondary-header__title text-center">{{post.title}}</h1>
					{{post.content}}
				</div>
			</div>
		</div>
	</div>

	{% include 'footer.twig' with {'pullup': '120'} %}
{% endblock %}

